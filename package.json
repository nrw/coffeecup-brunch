{
  "name": "coffeecup-brunch",
  "version": "1.3.0",
  "description": "Adds coffeecup support to brunch.",
  "author": "nrw",
  
  "main": "./lib/index",
  "scripts": {
    "prepublish": "coffee -o lib/ src/",
    "postinstall": "node setup.js postinstall",
    "test": "node setup.js test"
  },
  "engines": {
    "node": "~0.6.10 || 0.8 || 0.9"
  },

  "dependencies": {
    "coffee-script": "1.4.x",
    "coffeecup": "latest",
    "stylus": "latest"
  },

  "devDependencies": {
    "mocha": "latest",
    "chai": "1.1.0"
  }
}
